import { StyleSheet } from "react-native";

export const globals = StyleSheet.create({
  container: {
    flex: 1,
    display: "flex",
    alignItems: "center",
  },
  text: {
    color: "#eeeeee",
    textAlign: "center",
  },
  error: {
    color: "#e70008",
    marginBottom: -8
  }
});
