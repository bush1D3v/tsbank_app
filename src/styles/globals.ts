import { StyleSheet } from "react-native";

export const globals = StyleSheet.create({
  container: {
    flex: 1,
    alignItems: "center",
    backgroundColor: "#3178c6",
  },
  text: {
    color: "#eeeeee",
    textAlign: "center",
  }
});
